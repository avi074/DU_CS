library('shiny')

# Define UI for application
ui <- fluidPage(

    # Application title
    titlePanel("Database Simulator"),

  
)

# Define required server logic 
server <- function(input, output) {

    
}

# Run the application 
shinyApp(ui = ui, server = server)
